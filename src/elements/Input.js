import React from "react";
import styled from "styled-components";
const Input = (props) => {
  return (
    <React.Fragment>
      <input>인풋</input>
    </React.Fragment>
  );
};

export default Input;
